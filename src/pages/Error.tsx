import React from 'react';

const Error: React.FC = () => {
    return (
        <div>
            <h1 style={{color: 'red'}}>
                Page doesn't exist
            </h1>
        </div>
    );
};

export default Error;